import CheckboxesWidget from "./main";

/* istanbul ignore next */
CheckboxesWidget.install = function (Vue) {
    Vue.component(CheckboxesWidget.name, CheckboxesWidget);
};

export default CheckboxesWidget;
